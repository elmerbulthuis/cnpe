namespace: haproxy-unified-gateway

resources:
  - namespace.yaml
  - classes.yaml

helmCharts:
  - repo: https://haproxytech.github.io/helm-charts
    releaseName: haproxy-unified-gateway
    name: haproxy-unified-gateway
    version: 1.1.0
    valuesFile: values.yaml
