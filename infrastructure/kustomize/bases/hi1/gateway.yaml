apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: hi1
spec:
  gatewayClassName: haproxy
  listeners:
    - name: http
      protocol: HTTP
      port: 8080
      allowedRoutes:
        namespaces:
          from: Same
